import { createMuiTheme } from "@material-ui/core/styles";
const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#F0F0F0",
      main: "#212121",
      dark: "#868787",
      contrastText: "#F0F0F0",
    },
  },
});
export default theme;
